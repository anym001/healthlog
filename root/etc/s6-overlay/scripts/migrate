#!/command/with-contenv bash
# Bring the schema to head before the services start. Runs as the dropped
# user so any state it touches is owned correctly.
set -euo pipefail
cd /app/backend
exec s6-setuidgid healthlog python -m alembic upgrade head
